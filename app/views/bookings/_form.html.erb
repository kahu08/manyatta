<%= form_for [@house,@booking] do |f| %>
  <% if booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(booking.errors.count, "error") %> prohibited this booking from being saved:</h2>

     <ul>
      <% booking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


 <div class="field form-group">
    <%= f.label :checkin%>
      <br>
    <%= f.date_select :checkin, class:"form-control" %>
  </div>

 <div class="field">
    <%= f.label :checkout %>
    <br>
    <%= f.date_select :checkout, class:"form-control" %>
  </div>

 <div class="field">
    <%= f.label :occupancy %>
    <%= f.number_field :occupancy, class:"form-control" %>
  </div>

 <div class="actions field-action">
    <%= f.submit "Book Now" %>
  </div>
<% end %>
